basic onepage data control page with php
-create new database 'studentwebapp' in localhost 
-create table 'Student'
-create columns -> 
    id(pk), 
    fname(varchar),
    lname(varchar),
    snumber(int),
    department(varchar),
    birthdate(date)
-open index.php with your localhost.